import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://github.com/tintasytonersntn-coder/tintasytonersntn-coder.github.io.git',
  // ¡OJO! Como este es el repositorio principal, no necesitamos la línea "base".
});